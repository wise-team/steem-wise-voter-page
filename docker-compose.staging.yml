version: "3"

networks:
  proxy-tier:
    external:
      name: caddy-proxy

services:
  #§ d(data.config.votingPage.docker.services.frontend.name) + ':'
  site:
    build: .
    expose:
      #§ '- ' + d(data.config.votingPage.docker.services.frontend.port)
      - 8080
    networks:
      - proxy-tier
    restart: always